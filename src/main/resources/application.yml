logging:
  level:
    ru:
      yandex:
        practicum:
          filmorate: debug
spring:
  sql:
    init:
      mode: always
  datasource:
    url: 'jdbc:h2:file:./db/filmorate'
    driverClassName: org.h2.Driver
    username: sa
    password: password
  jpa:
    properties:
      hibernate:
        show_sql: true
